<UserControl x:Class="CloudSource.Playnite.GoogleDriveFolderPickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{DynamicResource TextBrush}"
             MinWidth="520"
             MinHeight="400">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="18" FontWeight="SemiBold" Text="Choose a game archive folder" />
            <TextBlock Margin="0,6,0,0" Opacity="0.75" TextWrapping="Wrap"
                       Text="Cloud Storage scans only the concrete folder you choose. My Drive root and the Shared with me collection cannot be selected." />
        </StackPanel>

        <Border Grid.Row="1" Margin="0,14,0,0" Padding="10" BorderThickness="1" BorderBrush="#40708090">
            <StackPanel>
                <TextBlock Opacity="0.7" Text="Currently configured" />
                <TextBlock Margin="0,3,0,0" FontWeight="SemiBold" Text="{Binding ExistingSelectionPath}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <DockPanel Grid.Row="2" Margin="0,14,0,8">
            <Button DockPanel.Dock="Left" MinWidth="74" Padding="10,4" Content="Back"
                    Command="{Binding BackCommand}" />
            <TextBlock Margin="12,5,0,0" FontWeight="SemiBold" Text="{Binding CurrentPath}" TextWrapping="Wrap" />
        </DockPanel>

        <Border Grid.Row="3" BorderThickness="1" BorderBrush="#40708090" Padding="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Folders}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Margin="0,0,0,5" Padding="12,9" HorizontalContentAlignment="Stretch"
                                    Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                    CommandParameter="{Binding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Margin="0,0,10,0" FontSize="18" Text="📁" />
                                    <StackPanel Grid.Column="1">
                                        <TextBlock FontWeight="SemiBold" Text="{Binding Name}" TextWrapping="Wrap" />
                                        <TextBlock Margin="0,2,0,0" Opacity="0.65" FontSize="11" Text="{Binding Description}" TextWrapping="Wrap" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Margin="12,0,2,0" VerticalAlignment="Center" FontSize="18" Text="›" />
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="4" Margin="0,8,0,0" Opacity="0.75" Text="{Binding Status}" TextWrapping="Wrap" />

        <StackPanel Grid.Row="5" Margin="0,16,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button MinWidth="140" Padding="12,6" Content="Use this folder"
                    Command="{Binding UseCurrentFolderCommand}" />
            <Button MinWidth="90" Margin="8,0,0,0" Padding="12,6" Content="Cancel"
                    Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
